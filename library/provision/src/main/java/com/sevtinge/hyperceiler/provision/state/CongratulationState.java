package com.sevtinge.hyperceiler.provision.state;

public class CongratulationState extends State {

    @Override
    public void onLeave() {
        super.onLeave();
            /*Utils.setNavigationBarFullScreen(mContext, false);
            if (mContext != null && !Utils.isGestureLineShow(mContext)) {
                Utils.hideGestureLine(mContext, false);
            }*/
    }
}
